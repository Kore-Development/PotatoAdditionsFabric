language: java
before_deploy:
  - mv ./build/libs/*.jar ./jar
deploy:
  provider: releases
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  file_glob: true
  file: jar/*
after_deploy:
  - rm -rf ./build
install: skip
before_script:
  - rm -rf ./build
script:
  - ./gradlew build
os: linux
