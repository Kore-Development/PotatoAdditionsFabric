language: java
before_deploy:
  - cd ./jar
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  on:
    branch: master
after_deploy:
  - cd ..
install: skip
before_script:
  - rm -rf ./build
script:
  - ./gradlew build
after_script:
  - mv ./build/libs/*.jar ./jar
os: linux
